---
title: 上位机参数下发
toc: true
mathjax: true
date: 2025-07-05 15:34:00
subtitle:
categories: 上位机模块
tags: 
 - 上位机
 - DSP
cover: https://s2.loli.net/2025/07/12/ZMLxznFsb7SRW13.jpg
dplayer: true
---
## 点击“加载回波”按钮回调

`DataLoadOpDlg.cpp`文件，入口函数：`void CDataLoadOpDlg::OnBnClickedButtonLoadStart()`点击“加载回波”按钮时，会调用这个函数，进行参数的下发。

下发参数的内存结构：`LOAD\_PARAM\_CFG\_CMD\_T`结构体的内存布局，上位机通过串口下发参数给FPGA。

```c
typedef struct _FRAME_HEAD_T
{
    WORD wHeadId1;      // 包头1
    WORD wHeadId2;      // 包头2
    WORD wHeadId3;      // 包头3
    DWORD dwFrameType;  // 帧类型
    DWORD dwRsv;        // 保留/扩展
    DWORD dwFrameLen;   // 帧长度
} FRAME_HEAD_T;

typedef struct _FRAME_END_T
{
    WORD wEndId1;       // 包尾1
    WORD wEndId2;       // 包尾2
} FRAME_END_T;

typedef struct _LOAD_PARAM_CFG_CMD_T
{
    FRAME_HEAD_T stHead;    // 帧头
    BYTE btChNum;           // 通道数
    BYTE btWorkMode;        // 工作模式
    WORD wADr_ChSel;        // AD通道选择
    WORD wDAr_ChSel;        // DA通道选择
    DWORD dwSita;           // 角度参数
    DWORD dwADf1Check1;     // AD校验1
    DWORD dwADf2Check1;     // AD校验2
    // ...（中间还有很多DWORD/WORD/BYTE类型的参数，见你的赋值代码）...
    WORD wLLRPrtWidth;
    WORD wLLRPrtNum;
    WORD wLLRCpiNum;
    // ...（更多PRT、CPI、门限等参数）...
    WORD wWaveGate1;
    WORD wWaveGate2;
    // ...（一直到wWaveGate20）...
    BYTE btWinNum;          // 窗口数
    BYTE btCrcCheck;        // CRC校验
    FRAME_END_T stEnd;      // 帧尾
} LOAD_PARAM_CFG_CMD_T; 

```

结构体定义在`mwRadarCtrlComm.h`中 名为`stParamCfgReq`，发送时将`stParamCfgReq`的内容使用“内存拷贝”实现，赋值完成后为适配下位机的字节序（大小端），对每个字段做了字节序翻转，赋值到stParamCfgReq1。

```c
stParamCfgReq1.stHead.wHeadId1 = publicFun::data_flipr(stParamCfgReq.stHead.wHeadId1);
```

## 下发波形参数结构

下面为上位机下发波形参数部分


| 字段名        | 类型  | 偏移 | 长度(字节) | 说明                  |
| ------------- | ----- | ---- | ---------- | --------------------- |
| wHeadId1      | WORD  | 0    | 2          | 包头1                 |
| wHeadId2      | WORD  | 2    | 2          | 包头2                 |
| wHeadId3      | WORD  | 4    | 2          | 包头3                 |
| dwFrameType   | DWORD | 6    | 4          | 帧类型                |
| dwRsv         | DWORD | 10   | 4          | 保留                  |
| dwFrameLen    | DWORD | 14   | 4          | 帧长度                |
| btChNum       | BYTE  | 18   | 1          | 通道数                |
| btWorkMode    | BYTE  | 19   | 1          | 工作模式              |
| wADr\_ChSel   | WORD  | 20   | 2          | AD通道选择            |
| wDAr\_ChSel   | WORD  | 22   | 2          | DA通道选择            |
| dwSita        | DWORD | 24   | 4          | 角度                  |
| dwADf1Check1  | DWORD | 28   | 4          | 雷达1通道1校正系数    |
| dwADf2Check2  | DWORD | 32   | 4          | 雷达2通道1校正系数    |
| …            | …    | …   | …         | …                    |
| dwADf1Check12 | DWORD | 116  | 4          | 雷达1通道12校正系数   |
| dwADf2Check12 | DWORD | 120  | 4          | 雷达2通道12校正系数   |
| wLLRPrtWidth  | WORD  | 124  | 2          | 超远脉冲PRT宽+脉冲宽  |
| wLLRPrtNum    | WORD  | 126  | 2          | 超远脉冲1cpi的prt个数 |
| wLLRCpiNum    | WORD  | 128  | 2          | 超远脉冲cpi个数       |
| wLRPrtWidth   | WORD  | 130  | 2          | 远脉冲…              |
| wLRPrtNum     | WORD  | 132  | 2          | 远脉冲…              |
| wLRCpiNum     | WORD  | 134  | 2          | 远脉冲…              |
| wLRPrtWidth   | WORD  | 130  | 2          | 远脉冲…              |
| wLRPrtNum     | WORD  | 132  | 2          | 远脉冲…              |
| wLRCpiNum     | WORD  | 134  | 2          | 远脉冲…              |
| wMRPrtWidth   | WORD  | 136  | 2          | 中脉冲…              |
| wMRPrtNum     | WORD  | 138  | 2          | 中脉冲…              |
| wMRCpiNum     | WORD  | 140  | 2          | 中脉冲…              |
| wSRPrtWidth   | WORD  | 142  | 2          | 近脉冲…              |
| wSRPrtNum     | WORD  | 144  | 2          | 近脉冲…              |
| wSRCpiNum     | WORD  | 146  | 2          | 近脉冲…              |
| wCellNum      | WORD  | 148  | 2          | 参考单元数+处理单元数 |
| dwDSNR        | DWORD | 150  | 4          | 信噪比参数            |
| wPrtWidthPD   | WORD  | 154  | 2          | PDprt宽度+脉宽        |
| wPrtNumPD     | WORD  | 156  | 2          | Pdcpi个数             |
| wCpiInterval  | WORD  | 158  | 2          | cpi发射间隔           |
| wWaveGate1    | WORD  | 160  | 2          | 门限1                 |
| ...           | ...   | ...  | ...        | ...                   |
| wWaveGate20   | WORD  | 198  | 2          | 门限20                |
| btWinNum      | BYTE  | 200  | 1          | 窗口数                |
| btCrcCheck    | BYTE  | 201  | 1          | CRC校验               |
| wEndId1       | WORD  | 202  | 2          | 包尾1                 |
| wEndId2       | WORD  | 204  | 2          | 包尾2                 |

## 下发引导信息结构

之后下发引导信息，下图为测试用串口下发引导信息结构。


| 字段名        | 类型  | 偏移 | 长度(字节) | 说明       |
| ------------- | ----- | ---- | ---------- | ---------- |
| wHeadId       | WORD  | 0    | 2          | 包头1      |
| dwTargetDis   | DWORD | 2    | 4          | 导引距离   |
| dwTargetAzi   | DWORD | 6    | 4          | 导引方位角 |
| dwTargetPitch | DWORD | 10   | 14         | 导引俯仰角 |

包头为0x3412，FPGA接收为0x1234，其余参数经过大小头颠倒后发FGPA，FPGA回传DSP引导信息，位置如下所示!![newmiso传输参数.png](https://s2.loli.net/2025/07/06/VwGElQ9bkmLJvD8.png)

以0x01020304，0x05060708，0x090A0B0C测试，上位机发送为![Snipaste_2025-07-06_15-45-58.png](https://s2.loli.net/2025/07/07/6lIa38Urmw9HMfL.png)

DSP内存接收如下![捕获.PNG](https://s2.loli.net/2025/07/05/nWvhzDbpXrBkulw.png)

DSP内存读取为![Snipaste_2025-07-06_15-42-56.png](https://s2.loli.net/2025/07/07/y4UWOEIjQYTAdx5.png)

DSP判读是否使用引导信息后，其中角度信息精度为0.01在播控阶段乘了100，在这里要除以100以变成正确值。回传参数结果为![Snipaste_2025-07-06_15-44-13.png](https://s2.loli.net/2025/07/07/VpdAqG4HOxbzaPB.png)

FPGA会一直发送最新的引导信息，如果没有就发送之前的信息参数，现还需修改上位机以发送不同引导信息进行测试。

## 两台根据导引进行回传与波门修正

由于每台导引头只能接收本台的导引信息即本台与目标的距离、方位角和俯仰角，如果不进行，本台处理另一台的回波前沿估计、波门前沿计算与距离测量将会一直处于不可修正状态。

现在将两台dsp1的测量结果传给各台的dsp2，传输结构如下![DSP回传newww.png](https://s2.loli.net/2025/07/07/n2egkVK9uAzhcEY.png)

dsp1内存传输如下![Snipaste_2025-07-06_15-45-12.png](https://s2.loli.net/2025/07/07/VATpH4kXyBIGw1b.png)

dsp2接收参数如下![mimo新接收参数.png](https://s2.loli.net/2025/07/07/yImhQ53HG6CFaKr.png)

以上为单基地接收数据，双基地数据结构一样紧邻排布。dsp2提取两经过引导信息修正的测距值，填充到dsp1回传上位机的距离值中，与此同时FPGA提取第五个值（回波前沿点2），这个值是经过引导信息纠正后的另一台的回波前沿，将这个数覆盖到下个cpi发送给dsp1参数的`0x81D00024`处对下个cpi双基地的测量进行修正。

即流程如下，FPGA处理第3个CPI时，DSP根据引导信息修正本台第2个CPI的测量值，同时修正另一台第二个cpi测量值，回传给FPGA修正第4个CPI的参数与处理。（注：`0x81D00024`点只有第二个cpi开始才会进行双基地修正值的覆盖）

## 引导信息的持续下发

为使得测试中导引头能够始终跟踪目标，防止跟错、跟偏的情况出现，在测试中给目标、两导引头均装配GPS设备，设备计算并转换得出三个关键值即导引距离、导引方位角、导引俯仰角，将这些参数传输给上位机，上位机现在通过串口按照一定频率下发给FPGA。

现在预计GPS设备的下发频率为**1s/次**，上位机下发频率为**50ms/次**。上位机始终下发最新引导信息，在未收到新引导信息时持续发送上次的引导信息，FPGA同理。

### 测试方法

<div class="line-red">×	下发回波信息时每个CPI的开头发送一次串口引导信息。</div>

1. 网口下发时在线程内调用串口会阻塞线程，导致发数卡住。
2. 网口下发为一次性发完，就算发送了多个cpi也用不到之前cpi的引导信息，FPGA只会发送最后一个cpi的引导信息。
3. 实测时不会通过网口发送回波

<div class="line-green">√  单独开设一个线程，在发完参数信息后以固定的频率下发引导信息。</div>

按如下方式设置测试引导信息的发送

```c
DWORD dwDis   = 10000 + 100 * pDlg->m_nGuideIndex;
DWORD dwAzi   = 1000  + 10  * pDlg->m_nGuideIndex;
DWORD dwPitch = 100   + 1   * pDlg->m_nGuideIndex;
```

**遇到问题**

1. 网络通信阻塞：网口一旦开启，大量的网口数据下发和接收会导致网络拥塞，影响串口发送
2. 串口缓冲区满：连续发送数据导致 串口缓冲区填满，造成阻塞
3. 线程资源竞争：多线程同时使用网口资源和UI资源，比如网口的数据接收，制表绘图功能
4. 串口超时设置过长：在 `SerialCom.cpp` 中设置了500ms的写超时

### 结果

打印调试信息：

```apache
// 检查串口是否可用
if (pDlg->m_SerialCom)
	{
		BOOL bResult = pDlg->m_SerialCom->SerialSend(&stGuide, sizeof(SendTrackInfoClass));
		if (bResult)
		{
			dwSendCount++;
		}
		else
		{
			dwFailCount++;
			// 发送失败时不额外延时，保持时序
		}
	}

	// 每100次发送统计一次性能
	if (pDlg->m_nGuideIndex % 100 == 0 && pDlg->m_nGuideIndex > 0)
	{
		DWORD dwCurrentTime = GetTickCount();
		DWORD dwElapsed = dwCurrentTime - dwLastTime;
		if (dwElapsed > 0)
		{
			TRACE(_T("Guide发送统计: 成功%d, 失败%d, 跳过%d, 平均间隔%.1fms\n"), 
				dwSendCount, dwFailCount, dwSkipCount, (float)dwElapsed / 100.0);
		}
		dwLastTime = dwCurrentTime;
		dwSendCount = 0;
		dwFailCount = 0;
		dwSkipCount = 0;
	}
```

**导致结果**

```apache
E:\radar8V17\mwRadarCtrl\DataLoadOpDlg.cpp(1380) : atlTraceGeneral - Guide发送统计: 成功100, 失败0, 平均间隔644.4ms
线程 35252 已退出，返回值为 0 (0x0)。
线程 31020 已退出，返回值为 0 (0x0)。
线程 34388 已退出，返回值为 0 (0x0)。
E:\radar8V17\mwRadarCtrl\DataLoadOpDlg.cpp(1380) : atlTraceGeneral - Guide发送统计: 成功100, 失败0, 平均间隔685.5ms
线程 20572 已退出，返回值为 0 (0x0)。
“mwRadarCtrl.exe”(Win32): 已卸载“C:\Windows\SysWOW64\WMASF.DLL”
“mwRadarCtrl.exe”(Win32): 已卸载“C:\Windows\SysWOW64\mfperfhelper.dll”
“mwRadarCtrl.exe”(Win32): 已卸载“C:\Windows\SysWOW64\WMVCORE.DLL”
“mwRadarCtrl.exe”(Win32): 已卸载“C:\Windows\SysWOW64\audiodev.dll”
“mwRadarCtrl.exe”(Win32): 已卸载“C:\Windows\SysWOW64\wpdshext.dll”
E:\radar8V17\mwRadarCtrl\DataLoadOpDlg.cpp(1380) : atlTraceGeneral - Guide发送统计: 成功100, 失败0, 平均间隔618.3ms
E:\radar8V17\mwRadarCtrl\DataLoadOpDlg.cpp(1380) : atlTraceGeneral - Guide发送统计: 成功100, 失败0, 平均间隔646.1ms
线程 24132 已退出，返回值为 0 (0x0)。
线程 28824 已退出，返回值为 0 (0x0)。
线程 7052 已退出，返回值为 0 (0x0)。
线程 17600 已退出，返回值为 0 (0x0)。
线程 32476 已退出，返回值为 0 (0x0)。
线程 19092 已退出，返回值为 0 (0x0)。
线程 30804 已退出，返回值为 0 (0x0)。
线程 7636 已退出，返回值为 0 (0x0)。
线程 7616 已退出，返回值为 0 (0x0)。
程序“[10856] mwRadarCtrl.exe”已退出，返回值为 0 (0x0)。
```

优化引导信息发送线程，减少系统调用和提高精度，减少对发送性能的影响，优化串口的缓冲区设置以提高性能。

例如：

```apache
// 大幅降低缓冲区阈值
if (ComStat.cbOutQue > 32)
{
	// 尝试清除输出缓冲区，强制发送
	PurgeComm(m_hCom, PURGE_TXCLEAR);
	return FALSE;
}
```

精简结果如下![Snipaste_2025-07-11_15-37-40.png](https://s2.loli.net/2025/07/11/3T5R7jx9HgG2skJ.png)

外部串口发送情况如下

{% dplayer url="/more/20250711_175031_1.mp4" %}

TX指示灯每闪烁一次表示发送成功一次引导信息，因为视频帧率有限无法反映真实的快速下发。

## DSP对引导信息的切换接收

测试10个中脉冲cpi，按照20ms发送一次下发引导信息，FPGA向DSP1下发参数图如下，其中红框所在数值为引导信息的三个内容。![Snipaste_2025-07-11_15-30-47.png](https://s2.loli.net/2025/07/12/WNDhSTA2e87jOlJ.png)

可见10个cpi接收到了6组不同的引导信息，因为在FPGA中使用时钟上下沿进行接收读取数据，在发送一个引导信息后可以未到达新的引导信息的上下沿从而发送前一个发送的数据。（注：这部分无法继续优化，因为windows系统的限制，最低的发送频率为15ms一次，上位机无法更快发数到达一个cpi一次切换的情况。）

## 无网口下发

外场时无需通过网口下发回波，只需网口接收回传，串口下发参数即可，调整上位机新增按钮回调相应功能如下。![Snipaste_2025-07-11_15-50-27.png](https://s2.loli.net/2025/07/12/zB1Gwng4Aoqkd5S.png)
